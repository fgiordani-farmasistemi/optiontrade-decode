.env
.venv/
__pycache__/
*.pyc
data/*.vtt
data/*.m4a
data/*.webm

# Database locale: ogni PC parte vergine
data/*.sqlite
data/*.sqlite-journal
data/*.sqlite-wal
data/*.sqlite-shm

# Vault chiavi API cifrate (DPAPI): mai versionato
data/secrets.json

# Backup automatici del database creati prima degli aggiornamenti
data/backups/

# Artefatti di build PyInstaller (intermedi). L'exe OptionTrade.exe e'
# invece versionato di proposito, cosi' arriva pronto nel download.
build/
dist/
*.spec
